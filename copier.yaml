# questions
project_name:
    type: str
    help: What is your project name?

is_package:
    type: bool
    default: false
    help: Is this going to be a Typst package?

package_name:
    type: str
    help: What is the package name?
    when: "{% if is_package %}true{% endif %}"
